<?php $this->headTitle('Lista')?>
	
<div class="grid_16">
	<div class="page-header">
		<h2> 
			<?php foreach ($this->linha as $disc){?>
				Lista de alunos com notas na turma de <?= $disc->Nome; ?>
			<?php }?>
		</h2>
	</div>
	
	<table class="table table-striped data-table" style="margin-bottom:0;">
		<thead>
			<tr>
				<th>#</th>
				<th>Nome</th>
				<th>Unidade 1</th>
				<th>Unidade 2</th>
				<th>Unidade 3</th>
				<th>Ação</th>
			</tr>
		</thead>
		<tbody>
			<?php $model = new Application_Model_DbTable_Matricula();?>
			<?php foreach($this->result as $lista){?>
				<tr>
					<td><?= $lista->idNota; ?></td>
					<?php $idUser = $lista->idUsuario_has_Turma;
						  $query = $model->getId($idUser); ?>
					<td><?= $query->findParentRow('Application_Model_DbTable_Usuario')->Nome; ?></td>
					<td><? if($lista->Unit1 == 0){echo '-';}else{echo $lista->Unit1;} ?></td>
					<td><? if($lista->Unit2 == 0){echo '-';}else{echo $lista->Unit2;} ?></td>
					<td><? if($lista->Unit3 == 0){echo '-';}else{echo $lista->Unit3;} ?></td>
					<td>
						<a class="btn btn-warning" href="<?= $this->url(
							array(
								'controller'	=>	'nota',
								'action'		=>	'delete',
								'idNota'		=>	$lista->idNota
							));?>">
								Excluir
						</a>
					</td>
				</tr>
		<?php }?>
		</tbody>
	</table>
</div>